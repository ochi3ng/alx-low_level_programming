#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	void print_alphabet_x10(void)
	char alph;

	while (count++ <= 9)
	{
		for (alph = 'a'; alph <= 'z'; alph++)
			_putchar(alph);
	
		_putchar('\n');
	}
}
